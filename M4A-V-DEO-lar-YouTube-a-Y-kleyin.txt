<!doctype html>
<html lang="tr">
<img src="https://upload.wikimedia.org/wikipedia/commons/b/b8/YouTube_Logo_2017.svg" alt="YouTube" width="150"> <h2></h2>
<p><strong>KudukoMüzikYapım</strong> google hesap ile giriş yaparak şifre gerektirmez %100 güvenilir: <strong></strong></p>
<p><strong>KudukoMüzikYapım</strong> kullanıcıların şunları yapmasını sağlar: <strong>m4a video dosyalarını YouTube'a yükleyin</strong>. Bunları bir görselle birleştirerek videolara dönüştürür.</p>

<p>Özellikle müzisyenler, yapımcılar ve kayıt stüdyoları için oldukça kullanışlıdır. Hizmet, 5 dakikalık bir m4a video klibini yalnızca 3 saniyede kodlayabilir ve m4a video dosyasını değiştirmeyerek <strong>kalite kaybı yaşanmasını önler</strong>.</p>

<p>KudukoMüzikYapım ayrıca <strong>toplu yükleme modu</strong>, zamanlanmış yüklemeler, ID3 etiket desteği gibi kayıpsız m4a video formatları gibi birçok ek özellik sunar.</p>

<p>2020'deki kuruluşundan bu yana, 100'den fazla kişi tarafından güvenilmektedir. <strong>1.500.000 kullanıcı</strong> ve tamamlanan <strong>38.000.000 yükleme</strong> ile başarısını kanıtlamıştır.</p>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>M4A VİDEO'ları YouTube'a Yükleyin</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      text-align: center;
      padding: 20px;
      background: #fff;
      color: #000;
    }

    input,
    button,
    select,
    textarea {
      width: 90%;
      max-width: 500px;
      margin: 10px auto;
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 14px;
      display: block;
    }

    button {
      background: #ff4b2b;
      color: #fff;
      cursor: pointer;
      transition: 0.2s;
      border: none;
    }

    button:hover {
      background: #ff416c;
    }

    .btn-link {
      display: inline-block;
      margin: 10px;
      padding: 10px 20px;
      background: #d00;
      color: #fff;
      text-decoration: none;
      border-radius: 6px;
      cursor: pointer;
    }

    #yt-desc,
    #log {
      text-align: left;
      background: #151515;
      color: #ddd;
      padding: 12px;
      margin: 10px auto;
      max-width: 500px;
      border-radius: 8px;
      font-size: 13px;
      overflow-x: auto;
    }

    #shareButtons {
      display: none;
      margin-top: 20px;
    }
  </style>
</head>

<body>

  <h2></h2>

  <input id="title" placeholder="Şarkı Adı — Sanatçı Adı" />
  <textarea id="desc" placeholder="Video açıklaması..."></textarea>

  <select id="privacy">
    <option value="private">Gizli</option>
    <option value="unlisted">Liste Dışı</option>
    <option value="public">Herkese Açık</option>
  </select>

  <input id="file" type="file" accept="video/*" />
  <input id="thumbnail" type="file" accept="image/*" />
  <input id="releaseDate" type="date" />

  <button id="auth">Google ile Giriş Yap</button>
  <button id="uploadYT" disabled>YouTube'a Yükle</button>
  <button id="uploadDrive" disabled>Drive'a Yükle</button>
  <button id="uploadDriveLink" class="disabled-link" onclick="window.open('https://kudukomuzikyapim.blogspot.com/p/mp3u-videoya-donustur.html', '_blank')">
    MP3'ü Videoya Dönüştür
  </button>

  <a class="btn-link" href="https://kudukomuzikyapim.blogspot.com/p/muzik-albumu-satn-al.html" target="_blank">Satın Al</a>

  <div>
    <div style="background:#2a2a2a; color:#fff; padding:10px; border-radius:10px; display:flex; align-items:center; gap:10px; margin-top:10px; justify-content:center;">
      <span id="copyText">@KudukoMüzikYapım.Official</span>
      <button onclick="copyToClipboard('copyText')" style="background:#ff0000; color:white; border:none; padding:5px 10px; border-radius:5px; cursor:pointer;">📋 Kopyala</button>
    </div>

    <div id="yt-desc">
      <p>Provided to YouTube by Kuduko Müzik Yapım</p>
      <p><strong id="sampleTitle">Şarkı Adı (Official Music)</strong> · Kuduko Müzik Yapım</p>
      <p id="sampleRepeat">Şarkı Adı (Official Music)</p>
      <p>℗ Kuduko Müzik Yapım</p>
      <br>
      <p>YouTube Kanalı: https://www.youtube.com/@KudukoMüzikYapım.Official</p>
      <p>İnstagram: https://instagram.com/kudukomuzikyapim</p>
      <p>TikTok: https://www.tiktok.com/@kudukomuzikyapm.official</p>
      <p>Snapchat: https://www.snapchat.com/@kuduko550</p>
      <p>Linkedin: https://www.linkedin.com/in/kudukom%C3%BCzikyap%C4%B1m/</p>
      <p>Discogs: https://www.discogs.com/release/33396431-Kuduko-M%C3%BCzik-Yap%C4%B1m-Plak-%C5%9Eirketi</p>
      <p>WhatsApp Grubumuza Katılın: https://grupbul.com/grup/645305</p>
      <p>Web site: https://kudukomuzikyapim.blogspot.com</p>
      <p>Mağaza: https://kudukomuzikyapim.company.site</p>
      <p>℗ & © Kuduko Müzik Yapım. Tüm hakları saklıdır.</p>
      <p id="releaseDateParagraph" style="margin-top: 10px; font-style: italic;"></p>
    </div>

    <button onclick="copyToClipboard('yt-desc')" style="margin-top:10px; padding:6px 12px; cursor:pointer; border:none; border-radius:5px; background:#ff0000; color:#fff;">📋 Açıklamayı Kopyala</button>

    <pre id="log"></pre>

    <iframe width="100%" height="300" src="https://www.youtube.com/embed/B5lL1mn3LpU" frameborder="0" allowfullscreen></iframe>

    <a class="btn-link" href="https://kudukomuzikyapim.blogspot.com/p/yaynlanan-m4a-videolar.html" target="_blank">Yayınlar Sayfası</a>
  </div>

  <!-- Paylaşım Butonları -->
  <div id="shareButtons">
    <h3>Videoyu Paylaş:</h3>
    <a id="whatsappBtn" class="btn-link" href="#" target="_blank">📲 WhatsApp</a>
    <a id="facebookBtn" class="btn-link" href="#" target="_blank">📘 Facebook</a>
    <a id="twitterBtn" class="btn-link" href="#" target="_blank">🐦 X (Twitter)</a>
    <a id="telegramBtn" class="btn-link" href="#" target="_blank">💬 Telegram</a>
    <a id="emailBtn" class="btn-link" href="#" target="_blank">📧 E-Posta</a>
    <a id="copyLinkBtn" class="btn-link" href="#" onclick="copyVideoLink()">🔗 Bağlantıyı Kopyala</a>
    <a id="embedBtn" class="btn-link" href="#" onclick="copyEmbedCode()">📎 Embed Kodunu Kopyala</a>
  </div>

  <script>
    function copyToClipboard(id) {
      const el = document.getElementById(id);
      const text = el.innerText || el.textContent;
      navigator.clipboard.writeText(text).then(() => alert('Kopyalandı!'));
    }
    document.getElementById('title').addEventListener('input', (e) => {
      const t = e.target.value || 'Şarkı Adı (Official Music)';
      document.getElementById('sampleTitle').innerText = t;
      document.getElementById('sampleRepeat').innerText = t;
    });
    // releaseDate alanındaki tarihi formatlayıp "Released on YYYY-MM-DD" şeklinde gösterir
    document.getElementById('releaseDate').addEventListener('change', (e) => {
      const dateStr = e.target.value;
      if (dateStr) {
        document.getElementById('releaseDateParagraph').textContent = `Released on ${dateStr}`;
      } else {
        document.getElementById('releaseDateParagraph').textContent = '';
      }
    });
    const CLIENT_ID = '831662029531-53aq67biqbtjjjv02fp971qau0lk8mp8.apps.googleusercontent.com';
    const SCOPES = 'https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/youtube';
    let accessToken = null;
    window.onload = () => {
      const script = document.createElement('script');
      script.src = 'https://accounts.google.com/gsi/client';
      document.body.appendChild(script);
      setupShareLinks();
    };
    document.getElementById('auth').onclick = () => {
      const tokenClient = google.accounts.oauth2.initTokenClient({
        client_id: CLIENT_ID,
        scope: SCOPES,
        callback: (tokenResponse) => {
          accessToken = tokenResponse.access_token;
          document.getElementById('uploadYT').disabled = false;
          document.getElementById('uploadDrive').disabled = false;
          document.getElementById('shareButtons').style.display = 'block';
        }
      });
      tokenClient.requestAccessToken();
    };

    function log(msg) {
      document.getElementById('log').textContent += msg + '\n';
    }
    document.getElementById('uploadYT').onclick = async () => {
      const fileInput = document.getElementById('file');
      const thumbnailInput = document.getElementById('thumbnail');
      const title = document.getElementById('title').value || 'Şarkı Adı (Official Music)';
      const description = document.getElementById('desc').value || '';
      const privacy = document.getElementById('privacy').value;
      if (!fileInput.files.length) {
        alert("Lütfen bir video dosyası seçin.");
        return;
      }
      const file = fileInput.files[0];
      log("YouTube'a Video Yükleniyor...");
      const metadata = {
        snippet: {
          title: title,
          description: description
        },
        status: {
          privacyStatus: privacy
        }
      };
      const form = new FormData();
      form.append('snippet', new Blob([JSON.stringify(metadata)], {
        type: 'application/json'
      }));
      form.append('video', file);
      try {
        const response = await fetch('https://www.googleapis.com/upload/youtube/v3/videos?part=snippet,status', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${accessToken}`
          },
          body: form
        });
        const result = await response.json();
        if (response.ok) {
          log("YouTube'a Video Yüklendi. Video ID: " + result.id);
          updateShareLinks(result.id);
          if (thumbnailInput.files.length) {
            await uploadThumbnail(result.id, thumbnailInput.files[0]);
          }
        } else {
          log("Hata: " + JSON.stringify(result));
        }
      } catch (err) {
        log("Yükleme hatası: " + err.message);
      }
    };
    async function uploadThumbnail(videoId, thumbnailFile) {
      log("YouTube'a Kapak Yükleniyor...");
      const form = new FormData();
      form.append('media', thumbnailFile);
      const res = await fetch(`https://www.googleapis.com/upload/youtube/v3/thumbnails/set?videoId=${videoId}`, {
        method: 'POST',
        headers: {
          'Authorization': `Bearer ${accessToken}`
        },
        body: form
      });
      const result = await res.json();
      if (res.ok) {
        log("YouTube'a Kapak Yüklendi.");
      } else {
        log("YouTube'a Kapak Yükleme hatası: " + JSON.stringify(result));
      }
    }

    function updateShareLinks(videoId) {
      const videoUrl = `https://youtu.be/${videoId}`;
      document.getElementById('whatsappBtn').href = `https://api.whatsapp.com/send?text=${encodeURIComponent(videoUrl)}`;
      document.getElementById('facebookBtn').href = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(videoUrl)}`;
      document.getElementById('twitterBtn').href = `https://twitter.com/intent/tweet?url=${encodeURIComponent(videoUrl)}`;
      document.getElementById('telegramBtn').href = `https://t.me/share/url?url=${encodeURIComponent(videoUrl)}`;
      document.getElementById('emailBtn').href = `mailto:?subject=Videoyu İzle&body=${encodeURIComponent(videoUrl)}`;
      window.currentVideoUrl = videoUrl;
    }

    function setupShareLinks() {
      const defaultUrl = window.location.href;
      document.getElementById('whatsappBtn').href = `https://api.whatsapp.com/send?text=${encodeURIComponent(defaultUrl)}`;
      document.getElementById('facebookBtn').href = `https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(defaultUrl)}`;
      document.getElementById('twitterBtn').href = `https://twitter.com/intent/tweet?url=${encodeURIComponent(defaultUrl)}`;
      document.getElementById('telegramBtn').href = `https://t.me/share/url?url=${encodeURIComponent(defaultUrl)}`;
      document.getElementById('emailBtn').href = `mailto:?subject=Videoyu İzle&body=${encodeURIComponent(defaultUrl)}`;
      window.currentVideoUrl = defaultUrl;
    }

    function copyVideoLink() {
      if (window.currentVideoUrl) {
        navigator.clipboard.writeText(window.currentVideoUrl)
          .then(() => alert('Video bağlantısı kopyalandı!'))
          .catch(() => alert('Kopyalama başarısız oldu.'));
      }
    }

    function copyEmbedCode() {
      if (window.currentVideoUrl) {
        const videoId = window.currentVideoUrl.split('/').pop();
        const embedCode = `<iframe width="560" height="315" src="https://www.youtube.com/embed/${videoId}" frameborder="0" allowfullscreen></iframe>`;
        navigator.clipboard.writeText(embedCode)
          .then(() => alert('Embed kodu kopyalandı!'))
          .catch(() => alert('Kopyalama başarısız oldu.'));
      }
    }
  </script>
</body>

</html>

